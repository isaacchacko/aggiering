import type { NextConfig } from "next";

/**
 * Mirror of `next.config.mjs`. The live config is `next.config.mjs`. Keep both in sync.
 */
const nextConfig: NextConfig = {
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
